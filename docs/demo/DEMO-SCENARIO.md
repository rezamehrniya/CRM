# سناریوی دمو Sakhtar CRM

**هدف:** نمایش توانایی پیگیری فروش، تبدیل لید به مشتری، و مانیتورینگ کامل وضعیت مشتری در **کمتر از ۵ دقیقه**، بدون نیاز به توضیح اضافه.

**دمو = پنل مدیر فروش** — تجربهٔ ورود و استفادهٔ مدیر فروش (B2B) در UI با همین tenant نمایش داده می‌شود.

**کاربر دمو:** مدیر فروش (نقش OWNER)  
**Tenant دمو:** `demo`  
**ورود:** لینک مستقیم `/t/demo/app` یا `/t/demo/app/login`  
(محلی: `http://localhost:8080/t/demo/app` یا `http://localhost:5173/t/demo/app`)

**ورود خودکار دمو:** برای tenant با slug `demo` نیازی به فرم لاگین نیست؛ با باز کردن `/t/demo/app` یا `/t/demo/app/login` ورود با کاربر `owner@demo.com` به‌صورت خودکار انجام می‌شود و کاربر به داشبورد هدایت می‌شود. (در صورت ناموفق بودن، فرم لاگین با همان اعتبار نمایش داده می‌شود.)

---

## مسیر گام‌به‌گام نمایش

### ۱. ورود به سیستم

- **مسیر:** `/t/demo/app` یا `/t/demo/app/login`
- **ورود خودکار:** بدون نیاز به وارد کردن رمز؛ در صورت نیاز دستی: ایمیل `owner@demo.com` / رمز `12345678`

**🟢 مشاهده:** داشبورد فروش با KPIها (لیدها، معامله‌ها، تسک‌های امروز).

---

### ۲. ایجاد یک لید جدید

- منو → **«مخاطبین»**
- دکمه **«افزودن مخاطب»**
- فرم:
  - نام: **علی رضایی**
  - موبایل: **09123456789**
  - مرحله: **«سرد»** (در صورت وجود فیلد Stage)

**🟢 مشاهده:**

- رکورد به لیست اضافه می‌شود.
- Stage در لیست قابل تغییر است (در صورت پیاده‌سازی).

---

### ۳. ثبت یک تماس پیگیری (Activity)

- کلیک روی ردیف **علی رضایی** → ورود به صفحه جزئیات مخاطب (Lead Detail).
- دکمه **«ثبت تماس»**:
  - نوع: **تماس**
  - نتیجه: **پیگیری موفق**
  - یادداشت: **معرفی کوتاه محصول**

**🟢 مشاهده:**

- Timeline به‌روز می‌شود.
- آخرین فعالیت قابل مشاهده است.

---

### ۴. ایجاد یک Task پیگیری

- دکمه **«ایجاد تسک»**:
  - عنوان: **ارسال پیش‌فاکتور**
  - تاریخ سررسید: **دو روز بعد**

**🟢 مشاهده:**

- تسک به لیست تسک‌های فروشنده اضافه می‌شود.

---

### ۵. تبدیل لید به مشتری

- روی لید **«علی رضایی»** → دکمه **«تبدیل به مشتری»**.
- سیستم تأییدیه می‌خواهد → **تأیید**.

**🟢 مشاهده:**

- در پنل **شرکت‌ها / مشتریان**، «علی رضایی» (یا شرکت مرتبط) اضافه شده است.
- اطلاعات او منتقل شده است.

---

### ۶. نمایش Customer 360°

- منو → **«شرکت‌ها»** → کلیک روی مشتری جدید.
- صفحه **مانیتورینگ کامل وضعیت مشتری**:
  - اطلاعات تماس
  - تسک‌های باز
  - فعالیت‌های اخیر
  - (در آینده: پروژه‌ها، پرداخت‌ها)

**🟢 مشاهده:**

- نمای یکپارچه وضعیت فروش و تعاملات.

---

### ۷. بررسی اشتراک (Billing)

- منو → **تنظیمات** (فقط برای OWNER).
- ورود به **«اشتراک و صورتحساب»**.

**🟢 مشاهده:**

- پلن فعال: مثلاً **Basic** (نمایشی).
- تعداد صندلی مصرف‌شده: **۱ از ۲۰**.

---

### ۸. خروج

- کلیک روی **آواتار** (بالا، سمت راست در RTL) → دکمه **خروج**.

**🟢 مشاهده:**

- برگشت به صفحه لاگین.

---

## ویژگی‌های کلیدی قابل نمایش در دمو

| ویژگی | قابل نمایش در دمو؟ |
|--------|---------------------|
| RTL کامل | ✅ |
| فرم Lead ساده و سریع | ✅ |
| پیگیری با تماس و تسک | ✅ (بعد از پیاده‌سازی Lead Detail + Activities + Tasks) |
| Timeline فعالیت‌ها | ✅ (بعد از پیاده‌سازی) |
| تبدیل لید → مشتری | ✅ (بعد از پیاده‌سازی Convert API) |
| Customer 360 (اطلاعات و پیگیری) | ✅ (بعد از پیاده‌سازی صفحه 360°) |
| سطح دسترسی (OWNER فقط برای تنظیمات) | ✅ |
| Billing Page | ✅ |
| خروج امن | ✅ |

---

## نکات دمو

- **زمان کل:** ۴–۶ دقیقه.
- مناسب برای **نمایش حضوری**، **ویدئو ضبط‌شده** یا **جلسه آنلاین**.
- اگر اتصال به پیامک/ایمیل ندارید → صرفاً فرم و Timeline را نمایش دهید.
- برای مشتری می‌توان **Customer Portal** (در حال توسعه) را به‌عنوان «آیتم بعدی» معرفی کرد.

---

## ارجاع

- **چک‌لیست QA این دمو:** [../tasks/QA-DEMO-SCENARIO.md](../tasks/QA-DEMO-SCENARIO.md)
- **معرفی فروش:** [../sales/DEMO-SALES-INTRO.md](../sales/DEMO-SALES-INTRO.md)
- **بکلاگ آماده‌سازی فروش:** [../tasks/SPRINT-SALES-PREP.md](../tasks/SPRINT-SALES-PREP.md)

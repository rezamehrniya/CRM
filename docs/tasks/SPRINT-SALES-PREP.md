# Backlog اجرایی — آماده‌سازی فروش Sakhtar CRM

**بازه:** ۲ هفته کاری (۱۴ روز)  
**ساختار:** ۲ فاز — Demo عملیاتی → فعال‌سازی فروش واقعی  
**هدف:** دموپذیر شدن، تبدیل لید به مشتری، و فعال‌سازی Tenant قابل فروش؛ فروشنده بتواند دمو کامل بدهد و CRM در عمل فروخته شود.

---

## Goal

- **فاز ۱:** Demo عملیاتی — Customer 360°، Lead Detail + Convert، Activities، Tasks، Billing در Settings، Dashboard فروشنده.
- **فاز ۲:** فروش واقعی — Tenant Setup، Subscription Guard (Read-only پس از انقضا)، Invoices، اعلان انقضا، (اختیاری) فرم دمو از لندینگ.
- **تحویل نهایی:** نسخهٔ Demo-Ready برای فروشنده + نسخهٔ Pilot-Ready برای مشتری واقعی؛ آمادهٔ قیمت‌گذاری و قرارداد و فرم خرید (خارج از کد).

---

## فاز ۱ — آماده‌سازی Demo (۵–۷ روز کاری)

| ID | اولویت | ماژول | Task | مالک | زمان | Done وقتی که… |
|----|--------|--------|------|------|------|----------------|
| SAK-010 | 🔺 | Customer 360° | ساخت صفحه مشاهده وضعیت مشتری (CRM + مالی + پروژه) | FE + BE | 2 روز | فروشنده بتواند مشتری را مانیتور کند |
| SAK-011 | 🔺 | Lead Detail | ساخت صفحه کامل لید + Timeline + تبدیل | FE | 1.5 روز | فروشنده از Lead به Client تبدیل کند |
| SAK-012 | 🔺 | Lead Conversion API | افزودن دکمه و route `POST /leads/:id/convert` | BE | 0.5 روز | تبدیل لید سمت سرور انجام شود |
| SAK-013 | 🔸 | Activities | افزودن `POST /activities` + فرم ثبت تماس/یادداشت | BE + FE | 1 روز | تماس ثبت شود و در Timeline بیاید |
| SAK-014 | 🔸 | Tasks | ساخت ماژول ساده: `GET/POST /tasks`, صفحه My Tasks | FE (+ BE در صورت نبود) | 1 روز | فروشنده بداند امروز چه کاری دارد |
| SAK-015 | 🔹 | Billing Page | صفحه Settings → Billing: نمایش پلن + صندلی | FE | 0.5 روز | دمو Billing قابل مشاهده باشد |
| SAK-016 | 🔹 | Dashboard Seller | KPIهای مخصوص فروشنده (leadهای من، تسک‌ها) | FE | 0.5 روز | فروشنده ورود کند و وضعیت خود را ببیند |

**یادداشت فاز ۱:**  
- Billing در Settings و نمایش پلن/صندلی از قبل پیاده شده است (SettingsBilling). در صورت نیاز فقط پولیش یا گسترش.  
- Lead و Activity و Task در Prisma موجود هستند؛ نیاز به endpoint و UI مطابق API-CONTRACT و PRD.

---

## فاز ۲ — فعال‌سازی فروش واقعی (۵–۶ روز کاری)

| ID | اولویت | ماژول | Task | مالک | زمان | Done وقتی که… |
|----|--------|--------|------|------|------|----------------|
| SAK-020 | 🔺 | Tenant Setup | `POST /api/tenants` یا Seed دستی + فرم اولیه | BE | 0.5 روز | tenant جدید ساخته شود |
| SAK-021 | 🔺 | Subscription Guard | بررسی تاریخ اشتراک → فعال/منقضی → حالت Read-only | BE | 1 روز | بعد از تاریخ انقضا، Write قفل شود |
| SAK-022 | 🔺 | Invoices | تعریف/تکمیل Invoice schema + `GET /invoices` | BE | 1 روز | صورتحساب نمایش داده شود |
| SAK-023 | 🔸 | Expiry Notification | آماده‌سازی Email/TG notification هنگام انقضا | BE | 1 روز | مدیر tenant مطلع شود |
| SAK-024 | 🔸 | Demo Form (optional) | فرم وب از لندینگ برای ساخت دمو tenant | FE + BE | 1 روز | تیم فروش بتواند از بیرون دمو بسازد |

**یادداشت فاز ۲:**  
- SubscriptionActiveGuard و مدل Subscription/Invoice از قبل در پروژه وجود دارند؛ تمرکز روی تکمیل رفتار Read-only و لیست Invoices و اعلان انقضا.  
- Tenant Setup: تعیین شود از طریق API داخلی (فقط Admin پلتفرم) یا فرم عمومی/لندینگ.

---

## Definition of Done (DoD)

- **فروشنده:**
  - بتواند Lead بسازد، پیگیری کند، تبدیل کند، و مشتری را مانیتور کند (Customer 360°).
- **مدیر tenant:**
  - Billing را ببیند؛ در صورت اتمام اشتراک، سمت Write قفل شود (Read-only).
- **سیستم:**
  - قابل فروش/استفاده در دنیای واقعی باشد.
  - بدون dependency غیرقابل تست.
  - مستندات + تست‌پذیری + پایداری حفظ شده باشد.

---

## Dependencies

- مدل‌های Prisma: Lead (در صورت تعریف)، Contact، Company، Deal، Activity، Task، Subscription، Invoice — موجود یا قابل گسترش.
- Auth و RBAC و Tenant Middleware از قبل پیاده شده‌اند.
- برای اعلان انقضا: سرویس Email یا Telegram (یا صف Job) در Backend.
- برای Tenant Setup از لندینگ: endpoint عمومی یا محافظت‌شده با API Key / Captcha.

---

## QA Checklist

- [ ] فروشنده می‌تواند Lead ایجاد کند، جزئیات و Timeline ببیند، و به مشتری تبدیل کند.
- [ ] صفحه Customer 360° برای یک مشتری وضعیت (فروش، مالی، فعالیت) را نمایش می‌دهد.
- [ ] تماس/یادداشت از فرم Activities ثبت می‌شود و در Timeline لید/مشتری دیده می‌شود.
- [ ] صفحه My Tasks (یا معادل) تسک‌های امروز/باز فروشنده را نشان می‌دهد.
- [ ] Dashboard فروشنده KPIهای «خود» (لیدهای من، تسک‌ها) را نشان می‌دهد.
- [ ] پس از انقضای اشتراک، درخواست‌های Write (POST/PATCH/DELETE) با 403 SUBSCRIPTION_EXPIRED پاسخ داده می‌شوند.
- [ ] مدیر tenant می‌تواند Billing و در صورت پیاده‌سازی Invoices را ببیند.
- [ ] (در صورت پیاده‌سازی) tenant جدید از طریق API یا فرم دمو ساخته می‌شود و قابل ورود است.

---

## Release Checklist

- [ ] Migrationهای دیتابیس (در صورت تغییر مدل) اجرا و تست شده‌اند.
- [ ] Envهای لازم (SMTP/Telegram برای اعلان، و غیره) در staging/production ست شده‌اند.
- [ ] رگرسیون اصلی: لاگین، نقش‌ها، مخاطبین، شرکت‌ها، تنظیمات، Billing بدون خرابی کار می‌کنند.
- [ ] اسناد مرتبط (API-CONTRACT، PRD، PERMISSION-MATRIX) در صورت تغییر به‌روز شده‌اند.

---

## تحویل نهایی (بعد از بکلاگ)

در پایان ۲ هفته:

| تحویل | توضیح |
|--------|--------|
| **Demo-Ready** | نسخهٔ قابل دمو برای فروشنده — Lead، Convert، Customer 360°، Activities، Tasks، Dashboard فروشنده، Billing در Settings |
| **Pilot-Ready** | نسخهٔ قابل استفاده برای مشتری واقعی — Tenant Setup، قفل Read-only پس از انقضا، Invoices، اعلان انقضا |
| **خارج از کد** | آماده‌سازی قیمت‌گذاری، فایل قرارداد، و فرم خرید (در لندینگ یا ابزار جدا) |

---

## ارجاع به اسناد

- **Spec و دسترسی:** [specs/RBAC-PANELS.md](../specs/RBAC-PANELS.md) · [specs/PRD-PANELS-USER-STORIES.md](../specs/PRD-PANELS-USER-STORIES.md) · [specs/API-CONTRACT.md](../specs/API-CONTRACT.md)
- **Billing و انقضا:** [specs/BILLING-SEATS.md](../specs/BILLING-SEATS.md)
- **وضعیت فعلی پروژه:** [PROJECT-STATUS.md](../PROJECT-STATUS.md)
